import { Client } from '../../src';

const PREFIX = 'clients-';
beforeAll(async () => {
    // if (isRecordMode()) {
    //     deleteFixtures(PREFIX);
    // }
});
describe('Client', () => {
    describe('UnifiOs', () => {});

    describe('non UnifiOs', () => {});
});
